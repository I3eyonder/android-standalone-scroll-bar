<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.hieupt.standalonescrollbar">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.StandaloneScrollBarSample">
        <activity android:name=".sample.NestedScrollViewActivity" />
        <activity android:name=".sample.HorizontalScrollViewActivity" />
        <activity android:name=".sample.ScrollViewActivity" />
        <activity android:name=".sample.HorizontalGridRecyclerViewActivity" />
        <activity android:name=".sample.VerticalGridRecyclerViewActivity" />
        <activity android:name=".sample.HorizontalRecyclerViewActivity" />
        <activity android:name=".sample.VerticalRecyclerViewActivity" />
        <activity android:name=".sample.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>